<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity">

    <ToggleButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/vibrateToggle"
        android:onClick="onVibrateClick"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/vibrate"
        android:text="@string/vibrate"
        android:textSize="20sp"
        android:layout_alignBaseline="@id/vibrateToggle" />

    <ToggleButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/soundToggle"
        android:onClick="onSoundClick"
        android:layout_below="@id/vibrateToggle"
        android:layout_alignLeft="@id/vibrateToggle"
        android:layout_alignStart="@id/vibrateToggle"
        android:layout_marginTop="20dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/sound"
        android:text="@string/sound"
        android:textSize="20sp"
        android:layout_alignBaseline="@id/soundToggle" />

    <Switch
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/aSwitch"
        android:textOn="@string/interval"
        android:textOff="@string/accel"
        android:layout_below="@+id/switchText"
        android:layout_marginTop="10dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/switchText"
        android:text="@string/switchText"
        android:textSize="20sp"
        android:layout_below="@id/soundToggle"
        android:layout_marginTop="30dp" />

</RelativeLayout>
